import { PartnerSession } from '@/components/partner/dashboard/partner-session';
export const metadata = { title: 'Partner support', robots: { index: false, follow: false } };
export default function Page() { return <PartnerSession page="support" />; }
